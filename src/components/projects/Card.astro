---
export interface Props {
  title: string;
  description: string;
  link: string;
  image: string;
  alt: string;
}

const { title, description, link, image, alt } = Astro.props;
---

<article>
  <a
    href={link}
    class="inline-flex items-center gap-1 text-lg font-semibold text-gray-800 hover:text-blue-500 transition-colors"
  >
    {title} ↗
  </a>
  <p class="mt-1 text-sm text-gray-500 max-w-md">
    {description}
  </p>
  <figure class="mt-4 overflow-hidden rounded-xl shadow-md">
    <img src={image} alt={alt} class="w-full object-cover" />
  </figure>
</article>
